[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mixer"
version = "0.1.0"
description = "Offline audio mixing (first iteration of a realtime mixer)"
requires-python = ">=3.10"
dependencies = ["numpy"]

[project.optional-dependencies]
dev = ["pytest"]
